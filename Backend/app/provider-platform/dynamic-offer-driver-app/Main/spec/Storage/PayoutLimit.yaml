imports:
  Merchant: Domain.Types.Merchant
  MerchantOperatingCity: Domain.Types.MerchantOperatingCity

PayoutLimit:
  tableName: payout_limit

  derives: "Generic, Show, Eq"

  fields:
    id: Id PayoutLimit
    merchantId: Id Merchant
    merchantOperatingCityId: Id MerchantOperatingCity
    dailyLimit: HighPrecMoney
    weeklyLimit: HighPrecMoney
    monthlyLimit: HighPrecMoney
    perTransactionMin: HighPrecMoney
    perTransactionMax: HighPrecMoney
    createdAt: UTCTime
    updatedAt: UTCTime

  constraints:
    id: PrimaryKey
    merchantOperatingCityId: SecondaryKey

  sqlType:
    dailyLimit: numeric(30,2)
    weeklyLimit: numeric(30,2)
    monthlyLimit: numeric(30,2)
    perTransactionMin: numeric(30,2)
    perTransactionMax: numeric(30,2)

  queries:
    findByMerchantOpCityId:
      kvFunction: findOneWithKV
      where: merchantOperatingCityId

  default:
    dailyLimit: "0.0"
    weeklyLimit: "0.0"
    monthlyLimit: "0.0"
    perTransactionMin: "0.0"
    perTransactionMax: "0.0"
