module: DriverToDriverDirectCalling
apis:
  - GET:
      endpoint: driver/getnumber/{rcNumber}
      params:
        rcNumber: Text
      auth: TokenAuth PROVIDER_TYPE
      response:
        type: DriverGetnumberResp

types:
  DriverGetnumberResp:
    name: Text
    mobileNumber: Text