imports:
  Text: Data.Text
  APISuccess: Kernel.Types.APISuccess

module: FRFSDriverRatingInternal

types:
  FRFSDriverRatingReq:
    merchantId: Text
    driverBadgeToken: Text
    bookingId: Text
    fleetNumber: Maybe Text
    gtfsId: Maybe Text
    driverRating: Maybe Int
    fleetRating: Maybe Int
    feedbackDetails: Maybe Text
    derive: "Show"

apis:
  # Internal endpoint called by rider-app (BAP) to deliver a shuttle driver rating.
  # Resolves the driver Person via operatorBadgeToken and feeds DriverStats.
  - POST:
      endpoint: /internal/frfs/driver/rating
      auth: NoAuth
      headers:
        token: Text
      request:
        type: API.Types.UI.FRFSDriverRatingInternal.FRFSDriverRatingReq
      response:
        type: Kernel.Types.APISuccess.APISuccess
