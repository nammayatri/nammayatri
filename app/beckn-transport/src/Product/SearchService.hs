-- {-# Beckn gateway interface #-}
-- {-# This is used to trigger GCM to all the aggregators registored #-}
-- {-# In future, aggregators will use their own method to assign drivers #-}

module Product.SearchService where