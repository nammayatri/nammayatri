module API.UI.Driver.Types (module Reexport) where

import Domain.Action.UI.Driver as Reexport
  ( CreatePerson (..),
    CreateVehicle (..),
    DriverEntityRes (..),
    DriverInformationRes (..),
    ListDriverRes (..),
    OnboardDriverReq (..),
    OnboardDriverRes (..),
    UpdateDriverReq (..),
    UpdateDriverRes,
  )
